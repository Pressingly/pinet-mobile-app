name: pinet_app

packages:
  - packages/**

scripts:
  analyze:
    exec: dart analyze .

command:
  bootstrap:
    environment:
      sdk: ">=3.3.1 <4.0.0"
      flutter: ">=1.17.0"

    dependencies:
      animations: ^2.0.11
      auto_route: ^7.9.0
      envied: ^0.5.3
      flutter:
        sdk: flutter
      flutter_localizations:
        sdk: flutter
      flutter_svg: ^2.0.10+1
      gap: ^3.0.1
      get_it: ^7.6.7
      intl: ^0.18.1
      local_auth: ^2.2.0
      lottie: ^3.1.0
      qr_code_scanner: ^1.0.1
      url_launcher: ^6.2.5

    dev_dependencies:
      auto_route_generator: ^7.3.2
      build_runner: ^2.4.8
      flutter_lints: ^3.0.0
      flutter_test:
        sdk: flutter
