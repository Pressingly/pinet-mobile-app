name: authentication
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  design:
    path: ../../core/design
  auto_route: ^7.9.0
  flutter:
    sdk: flutter
  qr_code_scanner: ^1.0.1

dev_dependencies:
  auto_route_generator: ^7.3.2
  build_runner: ^2.4.8
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
